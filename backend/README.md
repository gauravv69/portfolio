# Backend Placeholder

This project currently ships only the frontend login experience. A backend API is expected to live in this `backend/` directory but is not implemented here. Hook up your preferred server (Node/Express, Nest, Go, etc.) and wire auth endpoints before going live.

